// Package client provides the core SimConnect session, message dispatch,
// and ID allocation shared by all domain packages. Implementation is
// Windows-only; this file exists so that non-Windows builds and
// static-analysis tools see a non-empty package.
package client
